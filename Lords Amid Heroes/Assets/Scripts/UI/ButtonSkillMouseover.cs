using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonSkillMouseover : MonoBehaviour
{
    private SkillMouseOver skill;

    /*
    void OnMouseEnter()
    {
        Debug.Log("Enter 1");
        skill = FindComponent.FindComponentInChildWithTag<SkillMouseOver>(gameObject, "Skill");
        if(skill != null)
        {
            skill.Enter();
        }
    }

    void OnMouseOver()
    {
        if (skill != null)
        {
            skill.Over();
        }
    }

    void OnMouseExit()
    {
        if (skill != null)
        {
            skill.Exit();
        }
    }
    */
}
